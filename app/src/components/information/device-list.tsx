import React from 'react';

/**
 * List of supported devices
 */

const SupportedDeviceList: React.FC = () => (
  <>
    <h1>Supported devices</h1>
    <p>We support all modern browsers.... however....</p>
  </>
);

export {
  SupportedDeviceList,
};
